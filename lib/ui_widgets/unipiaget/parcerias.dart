import 'package:flutter/material.dart';
class Parcerias extends StatelessWidget {
  const Parcerias({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(

      ),
    );
  }
}
